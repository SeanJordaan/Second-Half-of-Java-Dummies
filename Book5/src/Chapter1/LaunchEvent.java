/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package Chapter1;

/**
 *
 * @author Sean
 */

/*
public class LaunchEvent implements Runnable //This class implements the Runnable interface
{

    private int start;
    private String message;

    public LaunchEvent(int start, String message)//The constructor accepts two parameters: an integer representing the 
                                                 //start time (in seconds) and a string message that’s displayed when the 
                                                 //time arrives. 
    {
        this.start = start;
        this.message = message;
    }

    public void run() {
        try {
            Thread.sleep(20000 - (start * 1000)); //In the run method, the Thread.sleep method is called to put the thread 
                                                  //to sleep until the desired countdown time arrives.
        } catch (InterruptedException e) {
        }
        System.out.println(message);//When the thread wakes up, it displays the message passed via its 
                                    //constructor on the console
    }
}
*/